submit50:
  files: &submit50_files
    - !exclude "__pycache__"
    - !exclude "node_modules"
    - !exclude "venv"
    - !require DESIGN.md
    - !require schema.sql
    - !require queries.sql
  style: false

check50:
  files: *submit50_files
